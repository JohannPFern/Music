# Instructions
```sh
python3 -m venv amt_venv
cd amt_venv
source bin/activate.sh
pip install -r requirements.txt
python3 -m ipykernel install --user --name=amt_env
jupyter-lab # start amt.ipynb in amt_env kernel
```
